# Application Configuration
NODE_ENV=production
PORT=5000

# JWT Configuration
JWT_SECRET=your-secure-jwt-secret-key-replace-this-in-production
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d

# Database Configuration
# Database Configuration
# For local dev with SQLite, uncomment the two lines below. Do NOT set USE_SQLITE in production.
# USE_SQLITE=true
# SQLITE_DB_PATH=./data/db.sqlite

# PostgreSQL (Render/Supabase/Neon) connection string. Required in production.
# Example: postgresql://user:pass@host:5432/dbname?sslmode=require
# DATABASE_URL=

# CORS Configuration
# Exact origin of your frontend (Vercel URL or custom domain). No trailing slash.
CORS_ORIGIN=https://your-frontend.vercel.app

# Security Settings
COOKIE_SECRET=your-secure-cookie-secret-replace-this-in-production
